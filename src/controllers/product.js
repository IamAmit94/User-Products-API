const joi = require('joi')

const {productDetail} = require('../validator/userValidator')
const productModel = require('../models/productModel')

const createProduct = async(req, res) => { 
    try {
        
    } catch (error) {
        
    }
}

const readProduct = async(req, res) => {
    try {
        
    } catch (error) {
        
    }
}

const updateProdcut = async(req, res) => {
    try {
        
    } catch (error) {
        
    }
}

const deleteProdcut = async(req, res) =>{

    try {
        
    } catch (error) {
        
    }
}

module.exports = {

    createProduct,
    readProduct,
    updateProdcut,
    deleteProdcut
}